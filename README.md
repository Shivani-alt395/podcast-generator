# Podcast Generator

A GitHub Action to generate a podcast RSS feed from a YAML file.

## Usage

\```yaml
- uses: Shivani-alt395/podcast-generator@v1.0
  with:
    email: 'you@example.com'
    name: 'Your Name'
\```

This action reads your podcast's YAML config and converts it into a valid RSS feed.
